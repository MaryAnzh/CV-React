import styled from 'styled-components';

export const FooterWrap = styled.footer`
	width: 100%;
	height: 100px;
	padding: var(--p-3);
	display: grid;
	justify-content: center;
	align-self: center;
`;
