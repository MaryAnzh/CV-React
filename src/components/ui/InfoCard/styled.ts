import styled from "@emotion/styled";

export const InfoCardWrap = styled.section`
    width: 100%;
    height: 100%;
    padding: var(--p-3);
    border-radius: var(--br-3);
    background: var(--main);
    gap: var(--p-3);
`;