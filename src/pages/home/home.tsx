import React from 'react';
import { PageTitle } from '../../AppStyle';
import { HomeStyle } from './home.styled';

export const Home = () => {
    return (
        <HomeStyle>
            <PageTitle>
            </PageTitle>
        </HomeStyle>
    );
}