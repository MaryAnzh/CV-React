import React from "react";

import { SizeCheckerStyle } from "./size-checker.styled";

export const SizeChecker: React.FC = () => {
    return (
        <SizeCheckerStyle>

        </SizeCheckerStyle>
    );
}