import React from 'react';
import { AnimationStyle } from './animation.styled';

export const Animation = () => {
    return (
        <AnimationStyle>
            Animation
        </AnimationStyle>
    );
}