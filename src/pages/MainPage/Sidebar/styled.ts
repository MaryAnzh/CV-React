import styled from "styled-components";

export const SidebarWrap = styled.aside`
    width: 360px;
    display: flex;
    flex-direction: column;
    gap: var(--p-3);
    padding: var(--p-3);
`;