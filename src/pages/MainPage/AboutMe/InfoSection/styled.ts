import styled from "@emotion/styled";
import { InfoCard } from "~components";

export const AboutMeWrap = styled(InfoCard)`
    display: flex;
    flex-direction: column;
    gap: var(--p-3);
`;